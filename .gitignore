# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# ── Secrets / env ───────────────────────────────────────
.env
backend/.env
frontend/.env

# ── Vector store (regenerate with `python backend/rag.py`) ─
backend/chroma_db/

# ── Node / frontend ─────────────────────────────────────
node_modules/
frontend/dist/
frontend/node_modules/

# ── Binaries / local tooling ────────────────────────────
# The LiveKit dev server is a 52MB platform binary — install it separately
# (https://github.com/livekit/livekit/releases) rather than committing it.
*.exe
livekit-server.exe

# ── Editor / OS ─────────────────────────────────────────
.DS_Store
.idea/
.vscode/
